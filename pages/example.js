import ExampleComponent from '../components/ExampleComponent';

export default function example() {
  return (
    <div>
      This is the /example page. This serves no purpose but to illustrate how links work.
      <ExampleComponent />
    </div>
  )
}
